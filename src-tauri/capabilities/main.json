{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Enable core IPC and dialog open for main window.",
  "windows": ["*"],
  "permissions": ["core:default", "dialog:allow-open"]
}
